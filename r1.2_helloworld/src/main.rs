fn main() {
//install : curl --proto '=https' --tlsv1.2 https://sh.rustup.rs -sSf | sh
// xcode-select --install (Mac OS C compiler)
//check version rustc --version and cargo --version
//get latest version rustup update
//uninstall rustup self uninstall

    println!("Hello, world!");
}
